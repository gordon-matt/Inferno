@using Microsoft.AspNetCore.Components.Web
@using Extenso
@using Extenso.AspNetCore.Mvc.Rendering
@using Extenso.Collections
@using Inferno.Identity
@using Inferno.Web
@using InfernoCMS

@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

@inject Microsoft.Extensions.Localization.IStringLocalizer T
@inject Inferno.Web.Configuration.SiteSettings SiteSettings
@inject Inferno.Web.IWorkContext WorkContext

@{
    bool isRTL = System.Globalization.CultureInfo.CurrentCulture.TextInfo.IsRightToLeft;
    string dir = isRTL ? "rtl" : string.Empty;
    string lang = WorkContext.CurrentCultureCode.LeftOf('-');
}

<!DOCTYPE html>
<html lang="@lang">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <base href="~/admin" />

    @RenderSection("meta", required: false)

    @if (isRTL)
    {
        <script src="https://cdn.jsdelivr.net/npm/@@laylazi/bootstrap-rtl@4.6.1-1/dist/js/bootstrap.min.js" integrity="sha256-nSnejUsmtuppwpg6tCemKOjplP9StX6HnaigOF+rqLE=" crossorigin="anonymous"></script>
    }
    else
    {
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.1/dist/css/bootstrap.min.css" integrity="sha256-DF7Zhf293AJxJNTmh5zhoYYIMs2oXitRfBjY+9L//AY=" crossorigin="anonymous">
    }

    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.12.0/css/all.css">
    <link href="_content/Blazorise/blazorise.css" rel="stylesheet" />
    <link href="_content/Blazorise.Bootstrap/blazorise.bootstrap.css" rel="stylesheet" />

    <link rel="stylesheet" type="text/css" href="/css/dashboard.css" />

    @RenderSection("styles", required: false)
</head>
<body dir="@dir">
    <nav class="navbar navbar-default navbar-fixed-top" role="navigation">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navbar-collapse-1">
                    <span class="sr-only">@T[LocalizableStrings.Dashboard.ToggleNavigation]</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand" href="#">Inferno CMS</a>
                @*@Html.ImageLink(
                    Url,
                    "/img/logo.png",
                    SiteSettings.SiteName,
                    "#",
                    imgHtmlAttributes: new { style = "max-width:200px;margin-top:18px" })*@
            </div>
            <div class="collapse navbar-collapse" id="navbar-collapse-1">
                @await Html.PartialAsync("AdminMenu")
                <ul id="secondary-nav" class="nav navbar-nav navbar-right">
                    @*@Html.ContentZone("AdminLanguageSwitch")*@
                    <li>
                        <a href="/">
                            <i class="fa fa-home"></i>
                            <span>@T[LocalizableStrings.Dashboard.Frontend]</span>
                        </a>
                    </li>
                    <li>
                        <a href="javascript:document.getElementById('logoutForm').submit()">
                            <i class="fa fa-power-off"></i>
                            <span>@T[InfernoIdentityLocalizableStrings.LogOut]</span>
                        </a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container">
        @if (WorkContext.Breadcrumbs.Any())
        {
            <div class="row">
                @await Html.PartialAsync("AdminBreadcrumbs")
            </div>
        }

        <div if.bind="router.isNavigating">
            <div class="splash">
                <div class="message">@T[InfernoWebLocalizableStrings.General.Loading]</div>
                <i class="fa fa-spinner fa-spin"></i>
            </div>
        </div>
        <div class="page-host">
            @RenderBody()
        </div>
    </div>

    @RenderSection("scripts", required: false)
</body>
</html>